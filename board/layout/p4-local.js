// @local-placement board/layout/p4_cluster.json
// Served by tools/copilot-shim (see its README, "A placement written by
// hand"): the file above is the finished layout for the parts it lists,
// the P4's decoupling rings, both crystals, the CSI series resistors, the
// flash and the core buck. Nothing else moves. The path is absolute because
// the shim reads it, not the MCP; regenerate the JSON with p4_cluster.py
// before running make_pcb_layout on this file.
