// @local-placement board/layout/csi_tracks.json
// Served by tools/copilot-shim: the file above carries tracks and vias, no
// parts, the sixteen hand-routed nets of csi_tracks.py (the MIPI lanes,
// the D-PHY reference, both crystals). Regenerate the JSON with
// csi_tracks.py on a fresh router artifact before running make_pcb_layout
// on this file; the script refuses if any pad it relies on has moved.
